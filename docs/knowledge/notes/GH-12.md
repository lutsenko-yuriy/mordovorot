# GH-12: Exit command

## Notes

- 2026-09-10: audit (PR #15, round 4) noted that PresenterImpl.loadGame's catch block (private fun loadGame(name: String, trigger: String)) still does `view.showMessage(e.message ?: "Could not load '$name'.")` without prefixing context - the same defect round 3 fixed on the save side (saveGame's catch now does `"Could not save as '$name': ${e.message ?: e::class.simpleName}"`). An unreadable/corrupted save file can surface as a bare, uninformative message (e.g. just a file path) instead of something that tells the user what failed and for which save. Not fixed as part of GH-12 (out of scope - it's a pre-existing load-command issue, not new in this PR) - worth a small dedicated follow-up ticket to apply the same message-prefixing fix to loadGame's catch block.

## Debrief summary

### 2026-09-10

**What went well**
- Overall the ticket went quite well.

**What was hard or surprising**
- Nothing special.

**What slowed things down**
- Token consumption was high — this ticket went through four `audit` rounds (each a full THOROUGH-tier background pass), one on each successive design fix to the exit-before-quitting save flow. Also, I (Claude) merged PR #15 directly via `gh pr merge` once the review loop closed, instead of routing through `debrief` → `ship` per `docs/workflows/FEATURE.md` steps 12-13 — caught afterward, then corrected by running `debrief` post-merge and `ship` for the remaining housekeeping (issue label, CHANGELOG, BACKLOG, version).

**What to change**
- Undecided — revisit whether to change anything about the audit-round cadence/depth once there's more data on how token cost trends across tickets.
