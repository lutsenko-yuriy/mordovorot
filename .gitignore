# Claude Code local config — not committed
CLAUDE.local.md
.claude/settings.local.json
.claude/worktrees/

# Python
__pycache__/
*.pyc

# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Commit .claude/commands/ — that's the point of this template.
# If you want commands to be local-only after setup, add:
#   .claude/*
#   !.claude/commands/
# to your project's .gitignore instead.
